# Brand assets (logo & icons)

| File | Purpose |
|------|---------|
| **`/logo.svg`** | Default logo (text "The Grand Gold"). Replace with your own SVG or add **`/logo.png`** and change the Logo component to prefer it. |
| **`/icons/icon-192x192.png`** | PWA icon and favicon. **Must be exactly 192×192 px.** A placeholder is committed; replace with your TG icon. Regenerate placeholder: `pnpm run generate:icon` (in apps/web). |

After replacing files, redeploy the web app. No code changes required unless you switch to logo.png.
